---
name: run-tests
description: Run full test suite. Use when tests need to be verified before committing, after code changes, or when user asks to run tests.
---

# Run Tests

Run the full Phantun test suite (unit + integration).

```bash
./scripts/run-tests.sh
```

Report: total passed, total failed, list any failures with test name and error.
